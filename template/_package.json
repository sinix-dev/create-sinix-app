{
  "name": "<%= name %>",
  "version": "0.1.0",
  "description": "A Sinix App",
  "scripts": {
    "dev": "snowpack dev --open none",
    "build": "snowpack build && sinix build"
  },
  "license": "MIT",
  "devDependencies": {
    "snowpack": "^2.11.0"
  },
  "dependencies": {
    "sinix": "^0.1.5"
  }
}
